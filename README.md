# Impact-Funds
This Python donations management system includes three user roles, Donor: Create accounts, login, and donate (with optional cause allocation). Recipient: Submit detailed help requests. Worker: View donations, manage requests, and analyze performance via Matplotlib charts. Data is persistently stored in JSON format.
